<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itgmm.mapper.UniversityMapper">

    <resultMap id="UserFavoriteUniversityMap" type="com.itgmm.pojo.UserFavoriteUniversity">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="university_id" property="universityId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="list" resultType="com.itgmm.pojo.University">
            select * from university;
    </select>

    <insert id="insert">
        INSERT INTO user_favorite_university (user_id, university_id, created_at)
        VALUES (#{userId}, #{universityId}, NOW())
            ON CONFLICT (user_id, university_id) DO NOTHING
    </insert>

    <delete id="deleteByUserIdAndUniversityId">
        DELETE FROM user_favorite_university
        WHERE user_id = #{userId} AND university_id = #{universityId}
    </delete>

    <select id="selectUniversityIdsByUserId" resultType="java.lang.Integer">
        SELECT university_id
        FROM user_favorite_university
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="selectAllByUserId" resultMap="UserFavoriteUniversityMap">
        SELECT id, user_id, university_id, created_at
        FROM user_favorite_university
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="countByUserIdAndUniversityId" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM user_favorite_university
        WHERE user_id = #{userId} AND university_id = #{universityId}
    </select>

    <select id="countByUniversityId" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM user_favorite_university
        WHERE university_id = #{universityId}
    </select>
</mapper>